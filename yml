GNU nano 7.2                                          playbook.yml
- name: Test Ansible Playbook
  hosts: all
  gather_facts: no

  tasks:
    - name: Print message
      debug:
        msg: "Hello Praveen, Ansible is working!"



        nano inventory.ini
[local]
localhost ansible_connection=local


nano playbook.yml
- name: Test Ansible Playbook
  hosts: all
  gather_facts: no

  tasks:
    - name: Print message
      debug:
        msg: "Hello Praveen, Ansible is working!"

ansible-playbook -i hosts.ini playbook.yml





mkdir -p roles/web/tasks
nano roles/web/tasks/main.yml
- name: Install Apache
  apt:
    name: apache2
    state: present
    update_cache: yes

- name: Start Apache
  service:
    name: apache2
    state: started
    enabled: yes

nano site.yml
- name: Deploy Web Server
  hosts: all
  become: yes

  roles:
    - web

ansible-playbook -i hosts.ini site.yml
